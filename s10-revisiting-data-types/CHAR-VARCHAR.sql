-- CHAR is fixed length and VARCHAR is variable length. CHAR always uses the same amount of storage space per entry, 
-- while VARCHAR only uses the amount necessary to store the actual text. 
-- The char is a fixed-length character data type, the varchar is a variable-length character data type.

-- CHAR is faster for fixed length text
    -- Sex: M/F
    -- State Abbreviations: CA, NY
    -- Yes/No Flags: Y/N

-- Otherwise, use VARCHAR
